# CLI 參考資料

> 最後更新：2026-04-26

## 來源清單

### 來源 1：官方 MCP 文檔
- **URL**：https://docs.openclaw.ai/cli/mcp
- **類型**：官方文件
- **擷取日期**：2026-04-26
- **可信度**：高（官方來源）
- **主要內容摘要**：
  說明 `openclaw mcp` 兩個主要功能：作為 MCP 服務器運行（`serve`）和管理 OpenClaw 擁有的出站 MCP 服務器定義（`list`、`show`、`set`、`unset`）。詳細說明了何時使用 `serve` vs 何時使用 `acp`，以及 MCP 服務器的使用場景。
- **用於文件的哪個部分**：
  用於 MCP CLI 參考資料部分的 `serve` 命令說明和場景解釋。
- **注意事項**：
  官方文件內容，但擷取可能不完整，建議直接參考官方獲取最新資訊。

### 來源 2：OpenClaw Cheatsheet 2026
- **URL**：https://openclawcheatsheet.com/
- **類型**：社群資源
- **擷取日期**：2026-04-26
- **可信度**：中（社群整理，需要驗證）
- **主要內容摘要**：
  聲稱包含 v2026.4.21 的 150+ CLI 命令、工作區文件、斜杠命令、通道設置、技能、內存、cron、安全等功能的完整參考資料。
- **用於文件的哪個部分**：
  提供了 CLI 命令分類和可用性的參考，但所有內容需要通過原始碼驗證。
- **注意事項**：
  需要與原始碼進行交叉驗證，確保命令和選項的準確性。

### 來源 3：OpenClaw Mega Cheatsheet
- **URL**：https://moltfounders.com/openclaw-mega-cheatsheet
- **類型**：社群資源
- **擷取日期**：2026-04-26
- **可信度**：中（社群整理，需要驗證）
- **主要內容摘要**：
  聲稱包含 150+ CLI 命令、工作區文件、內存系統、模型配置、鉤子、技能、多代理設置和故障排除的完整參考資料。
- **用於文件的哪個部分**：
  作為 CLI 功能分類和實際使用案例的參考，但需要原始碼驗證。
- **注意事項**：
  社群資源可能包含過時或不準確的信息，必須以原始碼為準。

## 研發筆記

### CLI 架構發現
通過分析原始碼發現：
- CLI 命令通過 `src/cli/program/register.subclis-core.ts` 註冊
- 每個命令群組都有對應的 register 檔案
- Config CLI 是最複雜的命令之一，包含多種模式（value、ref、provider、batch）
- Cron CLI 具有完整的 create/read/update/delete 操作和複雜的驗證邏輯

### 測試覆蓋分析
從原始碼分析發現：
- 大部分 CLI 命令都有對應的測試檔案（`.test.ts`）
- Config CLI 有完整的測試覆蓋，包括 edge cases 和錯誤處理
- Cron CLI 有互斥條件和邊界條件的測試
- 部分命令（如 skills）的測試覆蓋可能不足

### 文件完整性檢查
需要持續驗證：
- 所有列出的命令都能在原始碼中找到對應實作
- 所有選項都從 `.option()` 或 `.requiredOption()` 中提取
- 預設值和互斥規則都有原始碼依據
- 測試覆蓋的邊界條件都有明確標示

---

*此文件由 AI agent 自動生成並持續更新*